
<a href="<%= artist_path(artist)%>">
	 <img  class="c-artist-img"src="<%= artist.image.url(:thumb)%>">
</a>
<div class="channel-info">
	<a href="<%= artist_path(artist)%>">
	     <p class="channel_title"><%= artist.channel_title %></p> 
	  	 <p class = "artist_name text-muted"><%= artist.name %></p>
	</a>
</div>
	 
  

          
