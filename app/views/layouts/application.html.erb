<!DOCTYPE html>
<html>

<head>
  <title>EDM player</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

</head>
<body>
   <nav class="top-navigation">

      <div data-no-turbolink> <%= link_to "rTunes", root_path , class: "top-navbar-brand" , remote: true %></div>

      <% if current_user %>
        <div data-no-turbolink class="logged_in">
          <%= link_to current_user.name, user_path(current_user), class: "top-navbar", remote: true %>
          <%= link_to "logout", logout_path , class: "top-navbar"%>
        </div>
      <% else %>
        <div class="not_logged_in">
          <div id = "signup"><%= link_to "Sign Up", signup_path, class: "top-navbar" %></div>
          <div id="login"><%= link_to "Login", login_path , class: "top-navbar"%></div>
        </div>
      <% end %>
   </nav>

   <%= render 'layouts/player' %> 

   <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, id: "flash_#{name}" %>
   <% end %>


	<div class="container main">
		<%= yield %>
	</div>

<footer>

</footer>
</body>
</html>
